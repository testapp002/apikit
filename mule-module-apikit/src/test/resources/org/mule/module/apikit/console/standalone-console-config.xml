<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:apikit="http://www.mulesoft.org/schema/mule/apikit"
      xmlns:httpn="http://www.mulesoft.org/schema/mule/httpn"
      xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/httpn http://www.mulesoft.org/schema/mule/httpn/current/mule-httpn.xsd
        http://www.mulesoft.org/schema/mule/apikit http://www.mulesoft.org/schema/mule/apikit/current/mule-apikit.xsd">
      <!--xmlns:cors="http://www.mulesoft.org/schema/mule/cors"-->
        <!--http://www.mulesoft.org/schema/mule/cors http://www.mulesoft.org/schema/mule/cors/current/mule-cors.xsd"-->

    <apikit:config name="localhost" raml="org/mule/module/apikit/console/console.raml" consoleEnabled="false"/>

    <apikit:config name="bindAll" raml="org/mule/module/apikit/console/console.raml" consoleEnabled="false"/>

    <apikit:config name="embedded" raml="org/mule/module/apikit/console/console.raml" consoleEnabled="true" />

    <!--<cors:config name="defaultConfig"/>-->

    <httpn:listener-config name="httpListenerConfig">
        <httpn:listener-connection host="localhost" port="${serverPort}"/>
    </httpn:listener-config>

    <httpn:listener-config name="httpListenerConfig2">
        <httpn:listener-connection host="localhost" port="${serverPort2}"/>
    </httpn:listener-config>

    <httpn:listener-config name="httpListenerConfig3">
        <httpn:listener-connection host="0.0.0.0" port="${serverPort3}"/>
    </httpn:listener-config>

    <httpn:listener-config name="httpListenerConfig4">
        <httpn:listener-connection host="0.0.0.0" port="${serverPort4}"/>
    </httpn:listener-config>

    <httpn:listener-config name="httpListenerConfig5">
        <httpn:listener-connection host="localhost" port="${serverPort5}"/>
    </httpn:listener-config>

    <flow name="router">
        <httpn:listener config-ref="httpListenerConfig" path="/api/*"/>
        <!--<cors:validate publicResource="true" config-ref="defaultConfig" />-->
        <apikit:router config-ref="localhost"/>
    </flow>

    <flow name="console">
        <httpn:listener config-ref="httpListenerConfig" path="/konsole/*"/>
        <apikit:console config-ref="localhost"/>
    </flow>

    <flow name="consoleNoPath">
        <httpn:listener config-ref="httpListenerConfig2" path="/*"/>
        <apikit:console config-ref="localhost"/>
    </flow>

    <flow name="routerBindAll">
        <httpn:listener config-ref="httpListenerConfig3" path="/*"/>
        <!--<cors:validate publicResource="true" config-ref="defaultConfig" />-->
        <apikit:router config-ref="bindAll"/>
    </flow>

    <flow name="consoleBindAll">
        <httpn:listener config-ref="httpListenerConfig4" path="/*"/>
        <apikit:console config-ref="bindAll"/>
    </flow>

    <flow name="routerEmbedded">
        <httpn:listener config-ref="httpListenerConfig5" path="/*"/>
        <apikit:router config-ref="embedded"/>
    </flow>

    <flow name="get:/user">
        <set-property propertyName="Content-Type" value="application/json"/>
        <set-payload value="{ &quot;name&quot;: &quot;Lisa&quot; }"/>
    </flow>

</mule>
